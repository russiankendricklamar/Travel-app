---
phase: quick
plan: 260321-5bz
type: execute
wave: 1
depends_on: []
files_modified:
  - Travel app/Views/Map/TripMapView.swift
autonomous: true
requirements: [QUICK-5bz]

must_haves:
  truths:
    - "Map renders in dark color scheme regardless of system appearance"
    - "Transit overlay lines (buses, trains, metro) visible on map"
    - "Realistic elevation preserved on terrain"
    - "Map controls remain functional (compass, scale, user location, pitch)"
  artifacts:
    - path: "Travel app/Views/Map/TripMapView.swift"
      provides: "Dark map with transit overlay"
      contains: "preferredColorScheme(.dark)"
  key_links:
    - from: "TripMapView.mapContent"
      to: "MapStyle configuration"
      via: ".mapStyle modifier"
      pattern: "mapStyle.*standard.*elevation.*realistic"
---

<objective>
Force Apple Maps dark rendering with transit overlay on TripMapView.

Purpose: Match Apple Maps dark mode aesthetic — dark basemap with visible transit lines (green metro/bus routes), realistic elevation, clean minimal UI.
Output: TripMapView renders dark map with transit lines visible.
</objective>

<execution_context>
@/Users/egorgalkin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorgalkin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Travel app/Views/Map/TripMapView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark map with transit overlay</name>
  <files>Travel app/Views/Map/TripMapView.swift</files>
  <action>
In `mapContent` computed property (line 380), change the `.mapStyle` modifier from:
```swift
.mapStyle(.standard(elevation: .realistic, pointsOfInterest: .including([.museum, .nationalPark, .park, .restaurant])))
```
to:
```swift
.mapStyle(.standard(elevation: .realistic, pointsOfInterest: .including([.museum, .nationalPark, .park, .restaurant]), showsTraffic: true))
```

Then add `.preferredColorScheme(.dark)` modifier to the `mapContent` view — place it right after the `.mapStyle(...)` line (before `.mapControls`). This forces MapKit to render the dark basemap tiles regardless of system appearance.

NOTE: MapKit's `.standard` style in dark mode automatically shows transit lines (metro, bus routes) as colored overlays on the dark basemap — this is the native Apple Maps dark mode behavior. The `showsTraffic: true` parameter adds real-time traffic flow which complements the transit view.

Do NOT change `.mapControls` — keep MapScaleView, MapCompass, MapUserLocationButton, MapPitchToggle as-is. They render cleanly on dark maps already.

Do NOT change `elevation: .realistic` — must be preserved.

Do NOT apply `.preferredColorScheme(.dark)` to the entire body/NavigationStack — only to the map content, so the bottom sheet and other UI elements keep their current appearance.
  </action>
  <verify>
    <automated>cd "/Users/egorgalkin/Travel app" && xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -quiet build 2>&1 | tail -5</automated>
  </verify>
  <done>Map renders with dark basemap tiles, transit lines visible as colored overlays, realistic elevation preserved, all map controls functional. Bottom sheet and other UI unaffected by dark scheme.</done>
</task>

</tasks>

<verification>
Build succeeds. Visual: open TripMapView in simulator — map should show dark tiles with colored transit lines, 3D terrain, and all existing controls.
</verification>

<success_criteria>
- Map basemap is dark (dark roads, dark water, dark land)
- Transit lines (metro, bus) visible as colored overlays
- Elevation is realistic (3D terrain)
- Map controls (compass, scale, location, pitch) all functional
- Bottom sheet UI unaffected by dark color scheme
- Build compiles without warnings
</success_criteria>

<output>
After completion, create `.planning/quick/260321-5bz-apple-maps-style-dark-fullscreen-map-wit/260321-5bz-SUMMARY.md`
</output>
