---
phase: quick
plan: 260321-0ib
type: execute
wave: 1
depends_on: []
files_modified:
  - "Travel app/Views/Map/TripMapView.swift"
autonomous: true
requirements: [search-state-persistence]
must_haves:
  truths:
    - "Swipe-down to peek during active search does NOT reset searchQuery or searchResults"
    - "Swipe-down to peek during place detail DOES reset (Apple Maps behavior)"
    - "Sheet bounces to .half (not peek) when search is active to keep search bar visible"
    - "Cancel button (Отмена) still fully resets search and returns to peek/idle"
  artifacts:
    - path: "Travel app/Views/Map/TripMapView.swift"
      provides: "Conditional dismiss logic in onChange(of: sheetDetent)"
      contains: "hasActiveSearch"
  key_links:
    - from: "TripMapView onChange(sheetDetent)"
      to: "MapViewModel.dismissDetail()"
      via: "Only called for detail views or empty search state"
      pattern: "isInDetailMode"
---

<objective>
Fix search state being reset when user swipes bottom sheet down to peek. Apple Maps preserves search results on minimize — our app should too.

Purpose: Search results should persist when minimizing the sheet, not disappear.
Output: Updated onChange handler that conditionally preserves search state.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Conditional dismiss on sheet swipe-down</name>
  <files>Travel app/Views/Map/TripMapView.swift</files>
  <action>
    Replace the unconditional `dismissDetail()` call in `onChange(of: vm.sheetDetent)` with conditional logic:
    - If viewing place detail (placeDetail/searchItemDetail/aiResultDetail) → full reset via dismissDetail()
    - If active search (non-empty query/results/completer) → bounce sheet to .half to keep search visible
    - Otherwise → full reset via dismissDetail()
  </action>
  <verify>
    <automated>cd "/Users/egorgalkin/Travel app" && xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" build 2>&1 | tail -5</automated>
  </verify>
  <done>Sheet swipe-down preserves search state and bounces to .half. Detail views still dismiss normally. Cancel button still fully resets.</done>
</task>

</tasks>
