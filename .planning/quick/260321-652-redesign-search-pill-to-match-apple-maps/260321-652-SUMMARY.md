---
phase: quick
plan: 260321-652
subsystem: map-ui
tags: [map, search, ui, apple-maps, peek]
dependency_graph:
  requires: []
  provides: [compact-search-pill-peek-mode]
  affects: [MapBottomSheet, MapSearchContent, TripMapView]
tech_stack:
  added: []
  patterns: [conditional-background-by-detent, drag-gesture-on-content-area]
key_files:
  modified:
    - Travel app/Views/Map/MapBottomSheet.swift
    - Travel app/Views/Map/MapSearchContent.swift
    - Travel app/Views/Map/TripMapView.swift
decisions:
  - Peek pill uses Color(.secondarySystemGroupedBackground).opacity(0.97) for adaptive light/dark appearance
  - Drag gesture moved to content() area in peek mode since handle capsule is hidden
  - AI sparkles toggle always visible — no detent guard needed
metrics:
  duration: ~10min
  completed: 2026-03-21
  tasks_completed: 1
  tasks_total: 1
  files_modified: 3
---

# Quick Task 260321-652: Redesign Map Search Pill to Match Apple Maps

**One-liner:** Compact 74pt dark solid rounded pill in peek mode — Apple Maps style search bar with all-corner rounding, no drag handle, floating with padding, and always-visible AI toggle.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Redesign MapBottomSheet peek mode and show AI toggle in peek | f0712f1 | MapBottomSheet.swift, MapSearchContent.swift, TripMapView.swift |

## Changes Summary

### MapBottomSheet.swift
- Peek height: 120pt → 74pt
- Peek mode renders `RoundedRectangle(cornerRadius: 22)` with `Color(.secondarySystemGroupedBackground).opacity(0.97)` — solid fill that adapts to light/dark, with shadow
- Peek mode: drag handle `Capsule` hidden; drag gesture attached to `content()` area instead
- Peek mode: `padding(.horizontal, 16)` + `padding(.bottom, 4)` so pill floats above safe area
- Half/full mode: unchanged — `UnevenRoundedRectangle` top-corners-only, `.ultraThinMaterial`, drag handle visible

### MapSearchContent.swift
- AI sparkles toggle now always visible (removed `if vm.sheetDetent != .peek` guard)
- Inner capsule background conditionally shown: hidden in peek (sheet IS the bar), shown in half/full

### TripMapView.swift
- `safeAreaPadding(.bottom, isIdleMode ? 120 : 0)` → `74` to match new peek height

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- [x] MapBottomSheet.swift modified and committed
- [x] MapSearchContent.swift modified and committed
- [x] TripMapView.swift modified and committed
- [x] Build succeeded with no errors

## Self-Check: PASSED
