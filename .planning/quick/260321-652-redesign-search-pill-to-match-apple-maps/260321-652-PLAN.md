---
phase: quick
plan: 260321-652
type: execute
wave: 1
depends_on: []
files_modified:
  - Travel app/Views/Map/MapBottomSheet.swift
  - Travel app/Views/Map/MapSearchContent.swift
  - Travel app/Views/Map/TripMapView.swift
autonomous: true
requirements: [quick-652]
must_haves:
  truths:
    - "In peek mode, the search bar looks like a dark solid rounded rectangle at the bottom (no visible drag handle)"
    - "The bar shows magnifying glass + 'Поиск на карте' placeholder + AI sparkles toggle"
    - "Tapping the bar expands sheet to full and focuses search field"
    - "In half/full mode, sheet looks the same as before (ultraThinMaterial, drag handle visible)"
  artifacts:
    - path: "Travel app/Views/Map/MapBottomSheet.swift"
      provides: "Peek-specific appearance: reduced height, hidden drag handle, dark solid fill"
    - path: "Travel app/Views/Map/MapSearchContent.swift"
      provides: "Search field with AI toggle visible in peek mode"
  key_links:
    - from: "MapBottomSheet.swift"
      to: "MapSearchContent.swift"
      via: "content() closure renders search field inside the dark bar"
      pattern: "detent.*peek"
---

<objective>
Redesign the map search pill in peek mode to match Apple Maps: a compact dark solid rounded bar at the bottom with search icon, placeholder text, and AI sparkles toggle. No drag handle in peek. Tapping expands to full sheet as before.

Purpose: Visual polish — the peek search bar should feel native like Apple Maps.
Output: Updated MapBottomSheet (peek appearance), MapSearchContent (AI toggle in peek), TripMapView (bottom padding).
</objective>

<execution_context>
@/Users/egorgalkin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorgalkin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Travel app/Views/Map/MapBottomSheet.swift
@Travel app/Views/Map/MapSearchContent.swift
@Travel app/Views/Map/TripMapView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign MapBottomSheet peek mode and show AI toggle in peek</name>
  <files>Travel app/Views/Map/MapBottomSheet.swift, Travel app/Views/Map/MapSearchContent.swift, Travel app/Views/Map/TripMapView.swift</files>
  <action>
**MapBottomSheet.swift:**

1. Change `SheetDetent.peek` height from `120` to `74`.

2. Accept `detent` as a parameter in the body (it's already a Binding, so accessible). Conditionally render based on detent:
   - **Peek mode**: Hide the drag handle Capsule entirely (wrap in `if detent != .peek`). The drag gesture should still work on the entire sheet content area.
   - **Peek mode background**: Instead of `.ultraThinMaterial` with `UnevenRoundedRectangle(topLeadingRadius: 30, ...)`, use a fully rounded `RoundedRectangle(cornerRadius: 22)` filled with `Color(.secondarySystemGroupedBackground).opacity(0.97)` (solid dark in dark mode, light in light mode). Add subtle shadow. Use standard rounded corners on all 4 sides (not just top). Add horizontal padding (~16pt) so the bar floats above the bottom edge.
   - **Half/full mode**: Keep current appearance exactly — `UnevenRoundedRectangle` top corners only, `.ultraThinMaterial`, drag handle visible, no horizontal padding.

3. Move the drag gesture from just the handle capsule to cover the entire sheet content area in peek mode. In half/full mode, keep gesture on the handle only.

4. In peek mode, add `.padding(.bottom, 4)` to push the bar slightly above the safe area bottom edge, so it floats like Apple Maps.

**MapSearchContent.swift (searchFieldContent):**

1. Show the AI sparkles toggle button in ALL detent states (remove the `if vm.sheetDetent != .peek` guard on line 202). The toggle should always be visible.

2. In peek mode, remove the capsule background from the search field content (`.background(Capsule().fill(.quaternary.opacity(0.5)))` on lines 226-229). Since the sheet itself IS the dark bar in peek, the inner content should have no background. In half/full mode, keep the capsule background.

Conditional: `if vm.sheetDetent != .peek { Capsule().fill(.quaternary.opacity(0.5)) }` or use opacity.

**TripMapView.swift:**

1. Update `.safeAreaPadding(.bottom, isIdleMode ? 120 : 0)` to use `74` instead of `120` to match new peek height.
  </action>
  <verify>
    <automated>cd "/Users/egorgalkin/Travel app" && xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -skipMacroValidation build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - In peek mode: compact ~74pt dark solid rounded bar with all-corner rounding, no drag handle, floating with padding
    - Bar shows magnifying glass + "Поиск на карте" + AI sparkles toggle
    - Tapping bar expands to full sheet and focuses search
    - In half/full: identical to current appearance (ultraThinMaterial, drag handle, top-corner rounding only)
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
Build succeeds. In peek mode the search bar appears as a compact dark rounded pill at the bottom with search icon, placeholder, and AI toggle. Tapping expands to full. Half/full modes unchanged.
</verification>

<success_criteria>
- Peek mode: dark solid rounded bar (~74pt), no drag handle, all 4 corners rounded, AI sparkles visible
- Half/full: unchanged appearance (ultraThinMaterial, drag handle, top corners only)
- Tap-to-expand and drag gestures work correctly in all states
</success_criteria>

<output>
After completion, create `.planning/quick/260321-652-redesign-search-pill-to-match-apple-maps/260321-652-SUMMARY.md`
</output>
