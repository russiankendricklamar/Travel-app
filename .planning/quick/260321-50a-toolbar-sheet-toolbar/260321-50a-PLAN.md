---
phase: quick
plan: 260321-50a
type: execute
wave: 1
depends_on: []
files_modified:
  - Travel app/Views/Map/MapSearchContent.swift
  - Travel app/Views/Map/TripMapView.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "Today's places with visited status visible in search sheet idle state"
    - "Toolbar menu (layers/navigation/precipitation) accessible from sheet, not NavigationStack toolbar"
    - "Map is seamless — no toolbar clipping pins at top"
  artifacts:
    - path: "Travel app/Views/Map/MapSearchContent.swift"
      provides: "Today's places section + map controls menu"
    - path: "Travel app/Views/Map/TripMapView.swift"
      provides: "Seamless map without NavigationStack toolbar"
  key_links:
    - from: "MapSearchContent.swift"
      to: "MapViewModel"
      via: "vm.trip.todayDay?.sortedPlaces for today's places"
    - from: "MapSearchContent.swift"
      to: "MapViewModel"
      via: "vm.showPlaces/showRoutes/showPrecipitation toggles from moved toolbar"
---

<objective>
Move toolbar menu into search sheet, add today's places with visited status, make map seamless.

Purpose: The NavigationStack toolbar clips map pins at top. Moving controls into the sheet and showing today's places in idle search state improves usability.
Output: Seamless map with all controls in bottom sheet.
</objective>

<context>
@Travel app/Views/Map/MapSearchContent.swift
@Travel app/Views/Map/TripMapView.swift
@Travel app/Models/TripModels.swift (Place.category, Place.isVisited, PlaceCategory.systemImage, Trip.todayDay, TripDay.sortedPlaces)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add today's places section + map controls in MapSearchContent</name>
  <files>Travel app/Views/Map/MapSearchContent.swift</files>
  <action>
In MapSearchContent.swift, add two new sections visible when `isSearchFocused && vm.completerResults.isEmpty && vm.searchQuery.isEmpty` (same condition as category chips), placed AFTER the categoryChips block:

1. **Today's places section** — show when `vm.trip.todayDay` is non-nil and has places:
   - Section header: HStack with "Сегодня" bullet separator `vm.trip.todayDay!.cityName` + right-aligned visited count like "3/7" (visitedCount / total). Use `.font(.system(size: 13, weight: .semibold))`, `.foregroundStyle(.secondary)`, padding horizontal 16.
   - For each place in `vm.trip.todayDay!.sortedPlaces`:
     - Row: HStack with category icon (`place.category.systemImage`, size 14, in a 28x28 circle with `.quaternary` fill), place name (size 15, medium weight, lineLimit 1), Spacer, visited indicator.
     - Visited indicator: if `place.isVisited` show `checkmark.circle.fill` in `AppTheme.bambooGreen`, else `circle` in `.tertiary`. Size 18.
     - On tap: set `vm.selectedPlaceID = place.id`, dismiss keyboard (`isSearchFocused = false`), collapse sheet to peek (`vm.sheetDetent = .peek`).
     - Row padding: horizontal 14, vertical 10.
     - Dividers between rows (`.padding(.leading, 52)`).

2. **Map controls section** — always show when in the idle/focused state (below today's places or below chips if no today):
   - Small section header "Карта" same style as today header.
   - A row of icon buttons in an HStack (scrollable horizontal):
     - Layers button (line.3.horizontal.decrease.circle): opens a Menu with the same toggles as current toolbar (Места, Все города, GPS-треки, Поезда, Перелёты).
     - Precipitation button (cloud.rain): toggles `vm.showPrecipitation`.
     - Discover button (location.magnifyingglass): sets `vm.showDiscoverNearby = true`.
     - Zoom all button (map): calls `vm.zoomToAll()`.
   - Each button: 36x36 circle with `.quaternary.opacity(0.5)` fill, icon size 15. Label below icon, font size 10. Use VStack per button.
   - When precipitation is active, tint that button with `AppTheme.oceanBlue`.

Extract today's places and map controls as separate private computed properties (`todayPlacesSection`, `mapControlsSection`).
  </action>
  <verify>Build succeeds: xcodebuild -project "Travel app.xcodeproj" -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 16 Pro Max" build 2>&1 | tail -5</verify>
  <done>Search sheet idle state shows today's places with visit checkmarks and map control buttons below category chips</done>
</task>

<task type="auto">
  <name>Task 2: Remove NavigationStack toolbar to make map seamless</name>
  <files>Travel app/Views/Map/TripMapView.swift</files>
  <action>
In TripMapView.swift:

1. **Hide the navigation toolbar completely**:
   - Remove `.toolbar { toolbarContent }` (line 136).
   - Add `.toolbar(.hidden, for: .navigationBar)` to fully hide the navigation bar chrome. This replaces `.toolbarBackground(.hidden)` and `.toolbarColorScheme(.dark)` (remove both, lines 134-135).
   - Keep `.toolbar(isIdleMode ? .visible : .hidden, for: .tabBar)` — tab bar logic unchanged.

2. **Delete the `toolbarContent` computed property** (lines 396-482) entirely — this code now lives in MapSearchContent.

3. **Handle precipitation header**: The toolbar had a "ОСАДКИ" label when precipitation is active. Move this into a floating overlay in the ZStack (similar to NavigationHUDView pattern):
   - Only show when `vm.showPrecipitation` is true.
   - Position at top with `.padding(.top, 60)` (below status bar).
   - HStack with cloud.rain.fill icon + "ОСАДКИ" text, same styling as before.
   - Background: `.ultraThinMaterial` in a Capsule, with shadow.

4. Keep the NavigationStack wrapper (needed for sheets and .navigationBarTitleDisplayMode), just with toolbar fully hidden.
  </action>
  <verify>Build succeeds: xcodebuild -project "Travel app.xcodeproj" -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 16 Pro Max" build 2>&1 | tail -5</verify>
  <done>Map renders without any toolbar chrome at top, pins are not clipped, precipitation label floats as overlay, all menu functionality accessible from sheet</done>
</task>

</tasks>

<verification>
- Build succeeds without errors or warnings related to changed files
- Map view has no navigation bar visible at top
- Search sheet in idle+focused state shows: category chips, today's places with checkmarks, map control buttons
- Tapping a today's place zooms to it on map
- Layer toggles, precipitation, discover, zoom-all all work from sheet controls
</verification>

<success_criteria>
Map is seamless (no toolbar clipping pins), today's places appear in search sheet with visited status indicators, all former toolbar controls are accessible from within the bottom sheet.
</success_criteria>

<output>
After completion, commit with message: "feat(260321-50a): move toolbar to sheet, add today's places, seamless map"
</output>
