---
phase: quick
plan: 260321-00h
type: execute
wave: 1
depends_on: []
files_modified:
  - Travel app/Views/Map/MapViewModel.swift
  - Travel app/Views/Map/MapSearchContent.swift
  - Travel app/Views/Map/TripMapView.swift
  - Travel app/Views/Map/MapFloatingSearchPill.swift
  - Travel app/Views/Map/MapBottomSheet.swift
autonomous: true
requirements: [QUICK-SEARCH-UX]

must_haves:
  truths:
    - "Typing in search bar shows instant typeahead suggestions via MKLocalSearchCompleter (no 400ms debounce delay)"
    - "Tapping the floating search pill transitions sheet to .full with keyboard auto-focused"
    - "Cancel button appears when search is focused, tapping it cleanly returns to previous state (.peek + idle)"
    - "Search completer suggestions show title + subtitle rows, tapping one either resolves to MKMapItem or fills searchQuery"
  artifacts:
    - path: "Travel app/Views/Map/MapViewModel.swift"
      provides: "MKLocalSearchCompleter integration, completer results array, cancel/dismiss logic"
      contains: "MKLocalSearchCompleter"
    - path: "Travel app/Views/Map/MapSearchContent.swift"
      provides: "Cancel button, completer suggestion rows, updated search bar layout"
      contains: "completerResults"
    - path: "Travel app/Views/Map/TripMapView.swift"
      provides: "Sheet opens to .full on pill tap, onChange uses completer instead of MKLocalSearch debounce"
      contains: "sheetDetent = .full"
  key_links:
    - from: "MapViewModel"
      to: "MKLocalSearchCompleter"
      via: "NSObject delegate pattern (MKLocalSearchCompleterDelegate)"
      pattern: "MKLocalSearchCompleterDelegate"
    - from: "TripMapView.onChange(of: vm.searchQuery)"
      to: "MapViewModel.completer"
      via: "completer.queryFragment = newValue (replaces 400ms debounce + MKLocalSearch)"
      pattern: "queryFragment"
    - from: "MapSearchContent cancel button"
      to: "MapViewModel.dismissSearch()"
      via: "button action calls dismissSearch + unfocuses keyboard"
      pattern: "dismissSearch"
---

<objective>
Replace the current MKLocalSearch + 400ms debounce search mechanic with MKLocalSearchCompleter for instant typeahead, and fix the sheet/keyboard UX to match Apple Maps behavior: pill tap opens full sheet with keyboard, "Cancel" button replaces X for clean dismissal.

Purpose: The current search feels sluggish (400ms wait + full network search per keystroke) and the UX flow (sheet opens to .half, X button resets everything) does not match Apple Maps conventions.

Output: 4 modified files with Apple Maps-identical search UX.
</objective>

<execution_context>
@/Users/egorgalkin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorgalkin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Travel app/Views/Map/MapViewModel.swift
@Travel app/Views/Map/MapSearchContent.swift
@Travel app/Views/Map/TripMapView.swift
@Travel app/Views/Map/MapFloatingSearchPill.swift
@Travel app/Views/Map/MapBottomSheet.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MKLocalSearchCompleter to MapViewModel + cancel/dismiss state logic</name>
  <files>Travel app/Views/Map/MapViewModel.swift</files>
  <action>
1. Create a helper class `SearchCompleterDelegate: NSObject, MKLocalSearchCompleterDelegate` inside MapViewModel.swift (or as a separate nested/internal class). It holds a weak reference to MapViewModel and forwards `completerDidUpdateResults` and `completer(didFailWithError:)`.

2. Add new properties to MapViewModel:
   - `private let searchCompleter = MKLocalSearchCompleter()` — configured with `.pointOfInterest` and `.address` result types, `filterType = .localizingCompletions`
   - `private var completerDelegate: SearchCompleterDelegate?` — stored strong ref
   - `var completerResults: [MKLocalSearchCompletion] = []` — published for UI
   - `var isCompleterActive: Bool` — computed: `!searchQuery.isEmpty && searchedItem == nil && sheetContent != .searchResults`

3. In `init(trip:)`, create the delegate, wire it, assign `searchCompleter.delegate = completerDelegate`. Set `searchCompleter.resultTypes = [.pointOfInterest, .address]`.

4. Add method `updateCompleterQuery(_ query: String)`:
   - If query trimmed length < 2, clear completerResults and return
   - Set `searchCompleter.queryFragment = query`
   - If visibleRegion exists, set `searchCompleter.region = visibleRegion`

5. Delegate callback `completerDidUpdateResults`: set `self.completerResults = completer.results` on MainActor.

6. Add method `selectCompleterResult(_ completion: MKLocalSearchCompletion)`:
   - Create `MKLocalSearch.Request(completion: completion)` — this is the standard Apple API for resolving a completer result
   - Run the search, take first result MKMapItem
   - Call `selectSearchResult(item)` with that item
   - Clear `completerResults`

7. Update `dismissSearch()` to also clear `completerResults` and set `searchCompleter.queryFragment = ""`.

8. Update `dismissDetail()` to also clear `completerResults` and set `searchCompleter.queryFragment = ""`.

9. Keep `performMapSearch(query:)` and `submitSearch()` as-is — they are used for explicit submit (keyboard return) and AI mode. The completer is for live typeahead only in non-AI mode.
  </action>
  <verify>Build succeeds: `cd "/Users/egorgalkin/Travel app" && xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 16 Pro Max" build 2>&1 | tail -5`</verify>
  <done>MapViewModel has MKLocalSearchCompleter integration with delegate, completerResults array, selectCompleterResult method, and cleaned-up dismiss methods.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite search UX — full sheet on pill tap, cancel button, completer suggestions list</name>
  <files>Travel app/Views/Map/MapSearchContent.swift, Travel app/Views/Map/TripMapView.swift</files>
  <action>
**MapSearchContent.swift changes:**

1. Replace the X button (xmark.circle.fill) in `searchBar` with a "Отмена" text button:
   - Show "Отмена" when `isSearchFocused || !vm.searchQuery.isEmpty` (search is active)
   - On tap: call `vm.dismissSearch()`, set `isSearchFocused = false`
   - Style: `.font(.system(size: 16))`, `.foregroundStyle(AppTheme.sakuraPink)`
   - Place it OUTSIDE the search field rounded rect (after the closing of the background), not inside the HStack with the text field. This matches Apple Maps where "Cancel" is separate from the search pill.

2. Restructure the search bar area:
   ```
   HStack(spacing: 10) {
     // Search field with rounded rect bg (magnifyingglass + TextField + spinner + AI toggle)
     searchFieldContent

     // Cancel button (only when search active)
     if isSearchFocused || !vm.searchQuery.isEmpty {
       Button("Отмена") { ... }
     }
   }
   ```

3. Add completer suggestions section — show BETWEEN search bar and category chips / MKLocalSearch results:
   ```swift
   if vm.isCompleterActive, !vm.completerResults.isEmpty {
       completerSuggestionsList
   }
   ```

4. Create `completerSuggestionsList` view:
   - ScrollView(.vertical) with LazyVStack
   - Each row: magnifyingglass icon + VStack(title bold, subtitle secondary) + chevron.right
   - Title: `completion.title` with `.font(.system(size: 15, weight: .medium))`
   - Subtitle: `completion.subtitle` with `.font(.system(size: 13))` `.foregroundStyle(.secondary)`
   - On tap: `Task { await vm.selectCompleterResult(completion) }; isSearchFocused = false`
   - Dividers between rows (padding .leading 48)
   - `.frame(maxHeight: .infinity)` — fill available space in full sheet

5. Hide category chips when completer results are showing (user is typing): only show chips when `vm.completerResults.isEmpty && vm.searchQuery.isEmpty`.

6. Keep existing `searchResultsList` (MKLocalSearch results after explicit submit) and `aiSearchResultsList` as-is.

**TripMapView.swift changes:**

1. Change pill tap handler: open to `.full` instead of `.half`:
   ```swift
   MapFloatingSearchPill(vm: vm) {
       withAnimation(.spring(response: 0.35, dampingFraction: 0.85)) {
           vm.sheetDetent = .full
       }
       DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) {
           isSearchFocused = true
       }
   }
   ```

2. Replace the `onChange(of: vm.searchQuery)` block — remove the 400ms debounce + MKLocalSearch call. Instead, drive the completer:
   ```swift
   .onChange(of: vm.searchQuery) { _, newValue in
       guard !vm.isAISearchMode else { return }
       vm.searchTask?.cancel()
       vm.searchedItem = nil
       vm.updateCompleterQuery(newValue)
   }
   ```
   The explicit MKLocalSearch is now only triggered on keyboard submit (vm.submitSearch), not on every character.

3. The sheet should also show in non-idle states that include completer results. The existing `!isIdleMode` check for showing MapBottomSheet already covers this since once sheetDetent != .peek, isIdleMode is false.

4. In the `onChange(of: vm.sheetDetent)` handler, the existing swipe-down-to-peek logic calls `vm.dismissDetail()` which now also clears completerResults — this is correct behavior.
  </action>
  <verify>Build succeeds: `cd "/Users/egorgalkin/Travel app" && xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 16 Pro Max" build 2>&1 | tail -5`</verify>
  <done>Pill tap opens sheet to .full with keyboard focus. Typing shows instant MKLocalSearchCompleter suggestions. "Отмена" button cleanly dismisses search. No more 400ms debounce on every keystroke.</done>
</task>

</tasks>

<verification>
1. Build succeeds without errors or warnings related to map search files
2. Pill tap -> sheet goes to .full -> keyboard appears -> type "кафе" -> instant suggestions appear (no 400ms wait)
3. Tap suggestion -> resolves to map item, sheet goes to .half with detail
4. Tap "Отмена" -> returns to .peek idle state, search cleared, keyboard dismissed
5. AI search mode still works (toggled via sparkles button, uses submitSearch on return key)
6. Category chips still work (shown when search field is empty and idle)
7. Swipe sheet down to .peek still triggers full dismissal
</verification>

<success_criteria>
- MKLocalSearchCompleter provides instant typeahead suggestions as user types (no debounce)
- Sheet opens to .full when search pill tapped (keyboard + full list space)
- "Отмена" button replaces X for clean cancel-to-previous-state flow
- Tapping a completer suggestion resolves it via MKLocalSearch.Request(completion:) and shows detail
- Existing AI search, category chips, and submit-on-return behavior preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260321-00h-apple-maps-style-search-ux-in-maps/260321-00h-SUMMARY.md`
</output>
