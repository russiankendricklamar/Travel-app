---
phase: quick
plan: 260321-m0i
type: execute
wave: 1
depends_on: []
files_modified:
  - Travel app/Views/Map/MapBottomSheet.swift
  - Travel app/Views/Map/MapSearchContent.swift
  - Travel app/Views/Map/FloatingControlsOverlay.swift
autonomous: true
requirements: [QUICK-m0i]
must_haves:
  truths:
    - "Peek pill is 257pt wide and 36pt tall"
    - "Pill sits 28pt above the tab bar"
    - "Pill shows white Поиск text centered on solid black capsule"
  artifacts:
    - path: "Travel app/Views/Map/MapBottomSheet.swift"
      provides: "Peek dimensions (36pt height, 257pt width, 28pt bottom padding, solid black background)"
    - path: "Travel app/Views/Map/MapSearchContent.swift"
      provides: "Search label (magnifying glass 15pt + Поиск 17pt, white, centered)"
    - path: "Travel app/Views/Map/FloatingControlsOverlay.swift"
      provides: "Floating controls cleared above pill area (72pt bottom padding)"
  key_links:
    - from: "MapBottomSheet.swift"
      to: "MapSearchContent.swift"
      via: "content() closure in VStack"
      pattern: "content()"
---

<objective>
Verify and commit the search pill layout changes that match the reference screenshot.

Purpose: Uncommitted changes in MapBottomSheet.swift, MapSearchContent.swift, and FloatingControlsOverlay.swift already implement the correct dimensions. This plan verifies those values are correct, then commits.

Output: Committed implementation of 257pt x 36pt solid black search pill, 28pt above tab bar, with centered white Поиск label.
</objective>

<execution_context>
@/Users/egorgalkin/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

The uncommitted changes already contain:
- MapBottomSheet.swift line 11: peek height = 36
- MapBottomSheet.swift line 79: maxWidth in peek = 257
- MapBottomSheet.swift line 83: bottom padding in peek = 28
- MapBottomSheet.swift lines 88-109: solid black pill background with shadow, no material
- MapSearchContent.swift line 234: HStack spacing 6 in peek
- MapSearchContent.swift lines 237-238: magnifying glass 15pt, white opacity 0.6
- MapSearchContent.swift lines 246-247: Поиск text 17pt, white opacity 0.6
- MapSearchContent.swift line 313: vertical padding 9pt in peek
- FloatingControlsOverlay.swift line 35: .padding(.bottom, 72) above pill area
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dimensions in all three files</name>
  <files>
    Travel app/Views/Map/MapBottomSheet.swift,
    Travel app/Views/Map/MapSearchContent.swift,
    Travel app/Views/Map/FloatingControlsOverlay.swift
  </files>
  <action>
    Read each file and confirm the following values are present:

    MapBottomSheet.swift:
    - peek height returns 36 (not 56 or any other value)
    - maxWidth in peek is 257
    - bottom padding in peek is 28
    - Background uses Color.black (solid fill), NOT ultraThinMaterial or any material layer for the peek state. The UnevenRoundedRectangle at the peek layer has .fill(Color.black).

    MapSearchContent.swift:
    - In peek mode HStack, spacing is 6
    - Magnifying glass image is 15pt
    - "Поиск" text is 17pt
    - Both icon and text are white (or white with opacity)
    - Vertical padding in peek is 9pt

    FloatingControlsOverlay.swift:
    - Bottom padding to clear the pill area is 72pt

    If any value differs from the above, correct it in place. Do NOT change values that already match.
  </action>
  <verify>
    Build the app: open Xcode and press Cmd+B, or run:
    xcodebuild -scheme "Travel app" -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" build 2>&1 | tail -5

    Confirm build succeeds with no errors.
  </verify>
  <done>All three files contain the correct values and the project builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the verified changes</name>
  <files>
    Travel app/Views/Map/MapBottomSheet.swift,
    Travel app/Views/Map/MapSearchContent.swift,
    Travel app/Views/Map/FloatingControlsOverlay.swift
  </files>
  <action>
    Stage and commit only the three map view files. Use the conventional commit format.

    ```
    git add "Travel app/Views/Map/MapBottomSheet.swift" \
            "Travel app/Views/Map/MapSearchContent.swift" \
            "Travel app/Views/Map/FloatingControlsOverlay.swift"

    git commit -m "feat(map): match search pill layout to reference — 257pt wide, 36pt peek, 28pt above tab bar, solid black capsule"
    ```

    After committing, update STATE.md quick tasks table with this entry:
    | 260321-m0i | Match search pill layout to reference | 2026-03-21 | [commit sha] | [260321-m0i](./quick/260321-m0i-match-search-pill-layout-to-reference-25/) |
  </action>
  <verify>git log --oneline -1 shows the commit message above.</verify>
  <done>Commit exists. STATE.md quick tasks table updated.</done>
</task>

</tasks>

<verification>
After both tasks:
- git status shows the three map files no longer appear as modified
- git log -1 shows the feat(map) commit
- No build errors
</verification>

<success_criteria>
- MapBottomSheet.swift: peek = 36pt, maxWidth = 257pt, bottom padding = 28pt, solid black background
- MapSearchContent.swift: spacing 6, icon 15pt, text 17pt Поиск, white, vertical padding 9pt
- FloatingControlsOverlay.swift: bottom padding 72pt
- Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/quick/260321-m0i-match-search-pill-layout-to-reference-25/260321-m0i-SUMMARY.md` with:
- What was verified and committed
- Final dimension values confirmed
- Commit SHA
</output>
