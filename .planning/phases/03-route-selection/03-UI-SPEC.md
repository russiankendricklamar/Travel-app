---
phase: 3
slug: route-selection
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-20
---

# Phase 3 — UI Design Contract: Route Selection

> Visual and interaction contract for Phase 3. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> This is a SwiftUI/iOS project — shadcn gate not applicable.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (SwiftUI native) |
| Preset | not applicable |
| Component library | SwiftUI + AppTheme.swift + GlassComponents.swift |
| Icon library | SF Symbols (system icons) |
| Font | SF Pro (system default via `.system(size:weight:design:)`) |

Source: `Travel app/Theme/AppTheme.swift`, `Travel app/Theme/GlassComponents.swift`

---

## Spacing Scale

Declared values — all multiples of 4, matching `AppTheme` spacing constants:

| Token | Swift Constant | Value | Usage |
|-------|---------------|-------|-------|
| xs | `AppTheme.spacingXS` | 4pt | Icon gaps, label-to-value gaps inside cards, badge padding |
| sm | `AppTheme.spacingS` | 8pt | Inter-element gaps within carousel cards, card gap in carousel |
| md | `AppTheme.spacingM` | 16pt | Horizontal sheet padding, card internal padding, spacing before stats row |
| lg | `AppTheme.spacingL` | 24pt | Section gaps (pills → carousel → stats) |
| xl | `AppTheme.spacingXL` | 32pt | Not used in this phase |

Exceptions:
- Carousel horizontal padding: 16pt left/right (matches existing `transportModePills` padding)
- Route card width: 140pt fixed (scrollable, cards must be narrow enough to hint at overflow)
- Route card height: 88pt fixed (ETA + distance/transfers + label badge, compact)
- Card gap in carousel: 8pt (between cards — keeps 2+ cards visible simultaneously)
- Transport pill vertical padding: 8pt (matches existing pill minimum touch comfort)
- Touch targets: minimum 44×44pt for all interactive elements (transport pills, route cards, close button)

---

## Typography

All sizes use `.system(size:weight:)` — SF Pro, no custom font.

Declared weights: `.bold` and `.medium` only (2 weights maximum).

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| ETA value | 22pt | `.bold`, `.rounded` design | 1.1 | Primary ETA number inside route card |
| Card secondary | 13pt | `.medium` | 1.3 | Distance / transfer count label in card |
| Badge label | 10pt | `.bold` | 1.0 | «Быстрый» / «Короткий» pill badge |
| Section label | 12pt | `.medium` | 1.0 | «МАРШРУТЫ» carousel header label |
| Pill ETA | 10pt | `.medium`, `.rounded` design | 1.0 | ETA preview inside transport pills (existing, no change) |
| Pill mode label | 11pt | `.medium` | 1.0 | Mode label in inactive transport pills (existing, no change) |

Note: Existing route stats row uses 18pt bold rounded for ETA and 11pt for labels — the carousel ETA at 22pt is intentionally larger to be the hero number in each card. Section label moved from 11pt to 12pt to provide a clearer visual step above the 10pt badge label.

---

## Color

| Role | Token / Value | Usage |
|------|--------------|-------|
| Dominant (60%) | `.ultraThinMaterial` (system blur) | Route card background, sheet background |
| Secondary (30%) | `Color.primary.opacity(0.06)` | Inactive transport pill fill (existing pattern) |
| Accent (10%) | `AppTheme.sakuraPink` | Selected route card border, selected transport pill fill, badge text |
| Transport mode color | `mode.color` (from `TransportMode.color`) | Active pill fill, ETA text color in route card header icon |
| Destructive | `AppTheme.toriiRed` | Route calculation error message (existing, no change) |
| Loading skeleton | `Color.secondary.opacity(0.12)` | Skeleton shimmer fill during carousel loading state |

Accent (`AppTheme.sakuraPink`) reserved for:
1. Selected route card border stroke — 1.5pt, full perimeter
2. Selected route card subtle background tint — `sakuraPink.opacity(0.06)` behind material
3. «Быстрый»/«Короткий» badge text and background tint
4. NOT used on unselected cards, dividers, or the carousel scroll indicator

Source: CONTEXT.md — "выбранная — sakuraPink обводка"; AppTheme.swift AccentCardStyle pattern.

---

## Component Inventory

### New: RouteAlternativeCard

Location: `Views/Map/RouteAlternativeCard.swift`

States:
- **Default (unselected)**: `.ultraThinMaterial` background, white/0.12 stroke 0.5pt, shadow `black.opacity(0.06)` radius 8
- **Selected**: `.ultraThinMaterial` background, `sakuraPink` stroke 1.5pt full perimeter, `sakuraPink.opacity(0.06)` background overlay, shadow `sakuraPink.opacity(0.12)` radius 12
- **Loading**: skeleton placeholder — two shimmer rectangles (ETA slot 40×20pt, distance slot 60×13pt), animated with `opacity` keyframe 0.4→1.0→0.4, 1s duration, repeat forever

Layout (fixed 140pt wide × 88pt tall, vertical stack):
```
[transport icon 16pt, mode.color]   [badge pill if applicable]
[ETA 22pt bold rounded, mode.color]
[distance OR transfer count, 13pt medium, secondary]
```

Badge pill: 10pt bold, `sakuraPink` text, `sakuraPink.opacity(0.10)` fill, `cornerRadius: 6`, horizontal padding 4pt, vertical padding 4pt. Only one badge per set of alternatives (fastest gets «Быстрый», shortest gets «Короткий»; if same route wins both, show «Быстрый»).

Transit variant: replace distance line with "N пересадок" (N = `transitSteps.filter { $0.travelMode == "TRANSIT" }.count`). Format: "1 пересадка", "2 пересадки", "5 пересадок" (apply standard Russian declension).

Single-result variant: when API returns only 1 route, show exactly 1 card (no carousel illusion needed). Card is left-aligned, carousel still renders with 1 item.

Tap interaction: immediate `selectedRouteIndex` update on `MapViewModel`, polyline animates with `.withAnimation(.easeInOut(duration: 0.35))`.

### New: RouteAlternativesCarousel

Location: inline in `MapRouteContent.swift` (not a standalone file — inserted between transport pills and route stats row)

```
ScrollView(.horizontal, showsIndicators: false) {
    HStack(spacing: 8) {
        ForEach(alternatives) { RouteAlternativeCard }
    }
    .padding(.horizontal, 16)
}
```

Position in `MapRouteContent` VStack: after `transportModePills`, before `routeStatsRow`.
Vertical spacing: `.padding(.top, 12)` above carousel (existing spacing token, nearest multiple-of-4 above the carousel block), `.padding(.top, 16)` before stats row (matches `md` token).

Loading state: show 2 skeleton `RouteAlternativeCard` instances with shimmer animation while `isCalculatingRoute == true`.

Empty/error state: hide carousel entirely. `routeStatsRow` remains visible with the single `activeRoute` data.

### Modified: MapViewModel

New properties:
- `alternativeRoutes: [RouteResult]` — populated from `RoutingService.calculateRoute` returning array
- `selectedRouteIndex: Int` — default 0 (fastest, first returned)
- `selectedRoute: RouteResult?` — computed from `alternativeRoutes[safe: selectedRouteIndex]`

`activeRoute` continues to be used downstream by navigation — on route selection, update `activeRoute = selectedRoute`.

Polyline rendering: `TripMapView` reads `activeRoute` for polyline source — no change needed after `activeRoute` is kept in sync.

---

## Interaction Contracts

### Transport mode pill tap
1. User taps a different transport mode pill
2. `isCalculatingRoute = true` immediately
3. Carousel shows 2 skeleton cards
4. `RoutingService.calculateRoute` called for new mode → returns `[RouteResult]`
5. `alternativeRoutes` updated, `selectedRouteIndex = 0` (reset to fastest)
6. Skeleton cards replaced with real cards (no transition animation — instant swap)
7. `activeRoute` updated to `alternativeRoutes[0]`
8. Map polyline updates with `.easeInOut(duration: 0.35)`

### Route card tap
1. User taps a non-selected route card
2. `selectedRouteIndex` updates instantly
3. Tapped card animates to selected style: border width 0.5pt → 1.5pt, shadow changes — use `.animation(.spring(response: 0.25, dampingFraction: 0.85))` on card stroke and shadow properties
4. Previously selected card animates to default style simultaneously
5. Map polyline updates with `.withAnimation(.easeInOut(duration: 0.35))`
6. Route stats row updates to show selected route's ETA/distance (existing stats row reads from `activeRoute`)

### Polyline color on map
- Color: `mode.color` from `TransportMode.color` — matches transport pill active fill color
- Width: 5pt (existing pattern assumed from TripMapView)
- Only 1 polyline shown at a time (per CONTEXT.md decision)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | «НАЧАТЬ НАВИГАЦИЮ» (existing, no change — `location.fill` icon + tracking 1) |
| Carousel section label | «МАРШРУТЫ» (12pt, medium, tracking 1.5, secondary color — matches GlassSectionHeader pattern) |
| Badge: fastest route | «Быстрый» |
| Badge: shortest route | «Короткий» |
| Transfer count: 1 | «1 пересадка» |
| Transfer count: 2–4 | «N пересадки» |
| Transfer count: 5+ | «N пересадок» |
| Loading state | No text — skeleton cards only (avoid "Загрузка..." redundancy with existing ProgressView in stats row) |
| Single result state | No special label — just show the 1 card normally, no "единственный маршрут" copy |
| No alternatives available | Hide carousel entirely — no empty state copy needed (stats row still shows data) |
| Route error | Existing copy in `vm.routeError` — no change (red `exclamationmark.triangle.fill` + error string) |
| Transit unavailable | Existing «Транзит недоступен через Google в этом регионе» banner — no change |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| SwiftUI system | Native SwiftUI + MapKit | not required — first-party Apple frameworks only |
| SF Symbols | System icons via `.systemName` | not required — bundled with iOS |

No third-party registries. No package additions required for this phase.

---

## Phase-Specific Implementation Notes

These notes are for the executor and planner — not design tokens but implementation constraints derived from the design contract.

1. `RoutingService.calculateRoute` must be modified to return `[RouteResult]` instead of `RouteResult?`. The fastest route (index 0 from Routes API `routes[]` array) is selected by default.

2. Badge assignment logic: iterate `alternativeRoutes`, find index with minimum `expectedTravelTime` → «Быстрый», find index with minimum `distance` → «Короткий». If both minimums are at the same index, that card gets «Быстрый» only. Use strict less-than comparison (not ≤) — if two routes are equal on both metrics, no badge is awarded for that metric.

3. Transit mode via Google Directions returns a single route — `alternativeRoutes` will have 1 item. Carousel renders normally with 1 card. No special-case code needed; single-card display is the natural result.

4. The existing `routeStatsRow` reads from `vm.activeRoute`. After carousel card selection, `vm.activeRoute` must be updated to the selected `RouteResult`. This keeps the stats row, step list, and "НАЧАТЬ НАВИГАЦИЮ" button all consistent without refactoring.

5. Accessibility: `RouteAlternativeCard` must have `.accessibilityLabel` of "Маршрут [index+1]: [ETA], [distance or N пересадок][, Быстрый или Короткий если есть]" and `.accessibilityAddTraits(.isButton)`. Selected card adds `.accessibilityAddTraits(.isSelected)`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
